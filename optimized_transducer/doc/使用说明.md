### 1.编译算子

bash build.sh --pkg --soc=ascend910b --vendor_name=custom --ops=optimized_transducer --experimental

### 2.安装算子

./build_out/cann-ops-nn-custom-linux.aarch64.run

export LD_LIBRARY_PATH=/home/ma-user/Ascend/ascend-toolkit/latest/opp/vendors/custom_nn/op_api/lib/:${LD_LIBRARY_PATH}

#### 3.aclnn 接口测试

bash build.sh --run_example optimized_transducer eager cust --vendor_name=custom --experimental

#### 4.安装pybind模块

cd experimental/loss/optimized_transducer/pybind
pip install -e . --no-build-isolation

#### 5.执行python test.py

python test.py
